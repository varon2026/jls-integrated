# JLS 통합 관리 시스템 — 이어받는 사람에게

정상어학원(JLS) 학원 관리 시스템입니다. **개발자가 아닌 운영자 한 명이 실서비스를 혼자 관리합니다.**
지금 전국 6~7개 분원, 학생 수백 명이 매일 쓰는 시스템이니, 고칠 때 그 무게를 감안해 주세요.

> **처음 이 저장소를 맡았다면**: 이 문서를 끝까지 읽고, 최근 커밋 20개(`git log --oneline -20`)를
> 훑어보세요. 커밋 메시지에 "무엇을 왜 고쳤는지"가 한국어로 적혀 있습니다.
> 코드 주석도 마찬가지라, 그 둘이면 대화 기록 없이도 이어받을 수 있습니다.

---

## 1. 제일 먼저 — 배포 규칙 (어기면 안 됨)

- **절대 `main`에 직접 push하지 마세요.** 세션에 배정된 작업 브랜치에만 올립니다.
  (브랜치 이름은 세션마다 다릅니다. `git branch --show-current` 로 확인하세요.
   대개 `claude/...` 꼴이고, 시스템 안내에 적혀 있습니다.)
- 운영자가 VS Code에서 직접 받아서 배포합니다. **작업이 끝나면 아래 명령어를
  브랜치 이름을 넣어 그대로 적어주세요.** 운영자는 이걸 복사해서 붙여넣습니다:
  ```bash
  git pull origin <작업브랜치> && git push origin main
  ```
- 운영자가 이미 다른 브랜치에서 받아쓰고 있을 수 있으니, 새 세션 첫 작업 전에
  `git log --oneline origin/main -5` 로 최신 상태를 확인하세요.
  main에 없는 커밋이 브랜치에 남아 있으면 아직 배포 안 된 것입니다.
- 이 구조가 안전장치입니다. 실제로 잘못 고친 게 배포 전에 여러 번 걸러졌습니다.
- 요청하지 않으면 **PR을 만들지 마세요.**

**배포 방식**: GitHub Pages. `.github/workflows/`가 없어서 **Actions에 아무 기록도 안 남습니다.**
운영자가 "배포된 게 없다"고 하면 대개 push가 안 된 것이니, `git log origin/main`으로 확인하세요.
배포 확인은 GitHub → Settings → Pages 의 마지막 배포 시각.

---

## 2. 무엇으로 만들어졌나

- **순수 바닐라 JS.** 빌드 도구·프레임워크·번들러 없음. 파일 그대로 브라우저가 읽습니다.
- HTML은 문자열 이어붙이기 + 인라인 스타일로 만듭니다. 그 방식을 따라가세요.
- 데이터베이스는 **Supabase**. 브라우저에서 `sb.from(...)` / `sb.rpc(...)` 로 직접 부릅니다.

### 줄바꿈 문자를 꼭 지키세요

| 파일 | 줄바꿈 |
|---|---|
| `app.js`, `inwon-app/app.js` | **CRLF** |
| 그 외 전부 | LF |

CRLF 파일을 LF로 저장하면 diff가 파일 전체로 번집니다. 편집 스크립트를 쓸 때 주의하세요.

### 화면 구성

```
index.html  ← 통합관리 (app.js · inwon.js · books.js)
  ├─ 대시보드   인원현황 / 경영분석 / 전형현황 / 반배정표 / 반개설현황(본사만)
  ├─ 원무       레벨테스트 / 인원현황(아래 iframe) / 시험채점(grader.html)
  └─ 총무       교재관리(books-app) / 운영비(expense-app)

inwon-app/index.html  ← 학사관리 (별도 앱)
  통합관리 안에 ?embed=1 iframe 으로 뜹니다.
  세션을 sessionStorage['jls_session_v1'] + postMessage 로 넘겨서 로그인은 한 번만 합니다.
  통합관리에서 고른 학기를 ?sem= 으로 물려줍니다.

leveltest/  ← 신규생 레벨테스트 채점·성적표 (CHESS / ACE)
grader.html ← 재원생 시험채점 (DT / AT / 내신모의고사) — 위와 다른 시험입니다
```

**`app.js`(약 4천 줄)와 `inwon-app/app.js`(약 8천 줄)에 같은 이름의 함수가 양쪽에 있습니다**
(`banBu`, `banLevel`, `banParts`, `banLevelLabel`, `banRoom`, `banDayCode` 등).
**한쪽만 고치면 두 화면이 다른 답을 냅니다.** 반드시 양쪽을 같이 고치고, 같은 입력에 같은 답이 나오는지 대조하세요.

---

## 3. 학원 업무 흐름 (이걸 모르면 코드가 안 읽힙니다)

### 학기

봄(3~5) · 여름(6~8) · 가을(9~11) · 겨울(12~2). id는 `sem_2026_fall` 꼴.
겨울학기 1·2월은 **직전 해**로 칩니다 (2026년 1월 → `sem_2025_winter`).

### 한 학기가 도는 순서

```
레벨테스트 예약 → 응시 → 결과·레벨 배정 → 등록 or 다음학기 대기
                                              ↓
                         분원이 IMS에서 '전체명단' 엑셀 받아 업로드
                                              ↓
                    반배정 · 담임 배정 → 상담(HC1·HC2·MC1·MC2·MC3) → 인원마감
```

### 등록 · 대기 · 미등록 (레벨테스트 → 학기 명단)

**'등록완료'는 반배정·결제까지 끝나 그 학기 신규생 명단(`semester_records`)에 들어간 상태**입니다.
"다음 달에 올게요" 라고 약속만 한 학생은 등록도 미등록도 아닌 **대기**입니다.

- 대기는 다음 학기만이 아닙니다. **9월(가을)에 시험 보고 10월에 오는** 같은 학기 안의 대기도 있습니다.
  그래서 대기 학기 선택지 맨 앞에 '시험 본 학기' 자신이 들어 있습니다.
- **등록완료는 사람이 찍는 값이 아닙니다.** 실제로 명단에 들어갈 때 자동으로 붙습니다
  (학사관리 › 학생관리 › 신규생 추가 / 전체명단 업로드 / 원무 대기명단의 등록 버튼).
  예약에는 회원코드가 없어서(IMS 임시등록 중단) **이름으로 사람을 맞추고, 학교·학년이 어긋나면 뺍니다.**
  그래도 둘 이상 남으면 사람이 고르게 하고, 일괄 업로드에서는 그냥 건너뜁니다.
- 자동 반영 대상은 **'다음학기 대기'와 손 안 댄 '대기'(pending) 둘 다**입니다.
  분원은 대개 등록 여부를 안 건드리고 '대기'로 두기 때문입니다.
  단 손 안 댄 '대기'는 **이 학기 또는 직전 학기에 본 시험까지만** 봅니다 —
  몇 학기 전 흐지부지된 예약까지 끌어오면 엉뚱한 시험이 그 학기 전형으로 잡힙니다.
- `wait_semester`(대기 학기)는 **대기를 거쳐서 온 학생에게만** 남깁니다.
  같은 학기에 시험 보고 바로 등록한 학생에게 붙이면 거치지도 않은 대기를 거친 것처럼 보이고,
  전형 현황(=다음 학기 모집)에도 잘못 끼어듭니다.
- 명단에 없는데 등록완료로 두면 어드민 전형현황에 **'명단 누락'** 으로 뜹니다.
  그래서 명단에 없는 학기로 바꾸려 하면 자동으로 대기로 되돌립니다.
  (운정2 한혜수 건 — 8월 시험, 10월 등록 예정인데 등록완료+가을로 표시해서 계속 명단 누락이었습니다)

### 상담 회차

| 회차 | 대상 |
|---|---|
| HC1 · HC2 | **신규·복귀생만** (해피콜). 입학월 상관없음 |
| MC1 · MC2 · MC3 | 전원. 학기의 1·2·3번째 달에 대응. **입학월 이후 회차만** 대상 |

- **그 달에 못 끝내고 다음 달 초에 올리는 상담이 많습니다.** 그래서 학기 밖 날짜라도
  **그 달 1~7일이면 '전달 말일에 한 것'으로 보고 다시 판정**합니다 (`MC_GRACE_DAY`).
  6월 MC1을 7/1에 올리거나, 여름 MC3를 9/1에 올리는 경우입니다. 저장 날짜는 원래 날짜 그대로 둡니다.
  HC는 입학월 기준이라 구제하지 않습니다 — 9월에 올라온 HC는 가을 신규생 것이 맞습니다.
- 내신반은 한 학기에 **한 회차만** 봅니다. 그 회차는 사람이 직접 지정합니다(`exam_class_stages`).
  추정하지 마세요 — 업로드한 달로도, 상담 이력으로도 추정하다 두 번 다 크게 틀렸습니다.
- 정규반과 내신반을 같이 다니는 학생이 있습니다. `counseling_histories`에는 **반 구분이 없어서**
  어느 반에서 한 상담인지 알 수 없습니다. 이게 여러 버그의 뿌리였습니다.

---

## 4. 전체명단 업로드 — 여기가 버그의 8할

IMS에서 받은 엑셀을 분원이 손봐서 올립니다. **분원마다 표기가 제각각입니다.**

### 반 이름

표준: `[레벨] / 부코드 / 요일 / 학년 / (과목) / 강의실`

```
[DSA1] / FA1 / MWF / F                          운정1
[A1(4~6)] / FA2 / TT / E6 / 독해&문법&단어 / R   운정2
[IS2]SU1/MWF/IS2/J                              구형식 (구분자 없음)
[MA1(4~6) / FA3 / MWF / 중1 / O                 ← 닫는 대괄호 빠짐. 실제로 자주 옵니다
```

- 반 인식은 **`[` 로 시작하는지만** 봅니다. 닫는 대괄호는 안 봅니다.
- **내신반 판정이 먼저입니다** (`classKind`). 이름에 `내신`이 있으면 대괄호가 있어도 내신반입니다.
  예전엔 대괄호를 먼저 봐서 `[내신] 중1/천재(이)` 로 올린 분원의 내신반이 통째로
  **정규반으로 저장돼 정규 인원에 더해졌습니다** (장안 2026 가을 8개 반 85명).
  복구 SQL: `sql/fix_exam_class_kind.sql`
- 그래서 `banParts()`는 `]`가 없으면 **첫 `/` 앞까지**를 레벨로 보고 떼어냅니다.
- 요일 판단은 **`banDayCode()` 한 곳에서만** 하세요. 예전에 파싱과 라벨이 서로 다른 목록을 들고 있어
  `TT`(화목)를 한쪽만 알아듣는 일이 있었습니다.
- 학년은 `E5 E6 M1 M2 M3` 로 통일합니다. `초5 중1` 처럼 한글로 적어도 같은 것으로 봅니다. **고등부는 없습니다.**
- **반 라벨(`class_label`)은 DB에 저장되지만, 불러올 때 반이름에서 다시 만듭니다.**
  안 그러면 파싱 규칙을 고쳐도 명단을 다시 올리기 전엔 화면이 안 바뀝니다.

### 내신반 이름 (2026 가을 확정)

```
[내신]FA4/MWF/M3/O(천재이,미래엔문)
  │    │   │   │  │      └ 교과서 — 화면·집계에서 무시, 반 상세에만 작게
  │    │   │   │  └ 강의실
  │    │   │   └ 학년 (M1/M2/M3)
  │    │   └ 요일
  │    └ 시간대(부)
  └ 내신반 표시
```

정규반과 자리가 같습니다. 분원마다 `내신반(동아이/능률김)` `내신_M1` `[내신] 중1/천재(이)`
제각각이라 반배정표가 '부 미지정'으로 깨지고 인원까지 틀어졌습니다.

- **맨 뒤 괄호는 교과서**입니다 (`examBooks`/`stripBooks`). `banRoom`도 괄호를 떼고 강의실을 읽습니다.
- 화면 이름은 `examLabel()` 이 만듭니다 — `내신 · 월수금 4부 · M3 · O`.
  **불러올 때 이름에서 다시 만듭니다**(정규반과 같은 방식). 명단을 다시 올리기 전에도 화면이 정리됩니다.
- **내신반 회차(MC1/2/3) 지정은 반 이름에 붙어 있습니다** (`exam_class_stages.class_name`).
  그래서 **반 이름을 바꾸면 회차가 끊어져 상담률에서 조용히 빠집니다.**
  옛 이름과 새 이름을 자동으로 잇지 않습니다 — 잘못 이으면 상담률이 틀어지기 때문입니다.
  대신 분원 대시보드 맨 위에 **회차를 안 고른 내신반 경고**(`examStageWarn`)를 띄웁니다.

### 담임

`Rachel이정화` · `이동훈` · `Ashley 함채연`(공백 포함) — 전부 **한 사람**입니다.
`David김동환, Diana권다은` 처럼 **두 명이 적히면 업로드를 막습니다.**
두 명이 들어가면 그 반이 별개의 담임 그룹이 되어 대시보드·인원마감표·상담률이 둘로 쪼개집니다.

### 특이사항 칸

분원마다 말이 달라서 넓게 잡습니다:

| 판정 | 인식하는 말 |
|---|---|
| 신규 | `신규` `신규생` `신입` `신입생` `9월신규생` … (`/신규\|신입/`) |
| 복귀 | `복귀` `복귀생` `재등록` `재입학` `재입회` |
| 전입 | `전입` — 같은 칸의 분원 이름을 출신분원으로 읽습니다 (`장안 전입` → 장안) |

- **`복귀` 두 글자만 있으면 복귀생이 됩니다.** `6~7월온라인만 수강/8월복귀완료` 라고 적은 학생이
  복귀생 + 해피콜 대상이 되고, 입학일이 붙어 학기초에서도 빠졌습니다
  (장안 봄 마감 456 ≠ 여름 학기초 454 — `sql/fix_online_return.sql`).
  **직전 학기에 재원인데 이번 학기 신규·복귀로 올라오면 논리적으로 말이 안 됩니다.**
  나간 적이 없는데 돌아올 수는 없습니다. 전 분원에 9건 있었습니다.
- `origin`(신규/복귀 구분)은 **특이사항이 그렇게 말할 때만** 갱신합니다.
  빈칸으로 다시 올렸다고 원무에서 등록한 신규생이 기존생으로 떨어지면 안 됩니다.
- `target_type`(HC 대상 여부)은 **항상 `origin`에서 파생**시킵니다. 둘이 어긋나면 안 됩니다.

### 퇴원생 일괄업로드

- **다시 올려도 됩니다.** 이미 퇴원인 학생은 날짜·사유·메모가 새 파일 값으로 갱신되고,
  퇴원 이동이력도 새로 안 쌓이고 갱신됩니다(정정용). 먼저 지울 필요가 없습니다.
- **단, 새 파일에서 빠진 학생은 자동으로 재원으로 안 돌아옵니다.**
  학생 관리 › 퇴원생 상태 변경에서 `재원 복귀`로 한 명씩 되돌려야 합니다.
- **퇴원사유는 본사가 10개로 확정했습니다** (`WITHDRAW_REASONS`, 조부장님).
  타학원 이동 / 담임 불만 / 스케줄(시간이동 불가)·폐강 / 교우 관계 /
  이사·어학연수·캠프·여행(장기) / 졸업 / 차량 불만 / 학원비 부담 / 성적 불만족 / 건강상의 문제.
  **`기타`가 없는 게 일부러 그런 겁니다.** 늘리거나 줄이지 마세요.
- **엑셀 표기는 넓게 읽습니다** (`WD_REASON_ALIAS` + `wdNorm`). 공백뿐 아니라 괄호·빗금도 떼고 봅니다 —
  `스케쥴(시간이동불가)` 처럼 적어 와도 알아듣습니다.
  예전엔 라벨과 글자 하나까지 같아야 알아들어서, 띄어쓰기가 있는 `개인 사유` 때문에
  분원이 붙여 쓴 `개인사유`가 전부 `기타`로 떨어졌습니다(남동탄 여름 재업로드).
- **못 알아본 사유·빈칸은 아무 데나 넣지 않고 비워 둡니다.** `기타`가 없어졌기 때문입니다.
  대신 결과창에 이름까지 보여줍니다. 예전엔 빈칸을 `개인 사유`로 몰래 채웠습니다.
- 목록이 바뀌기 전 값(`personal` `burden` `other`)은 `WD_LEGACY_REASONS` 로
  **화면에 `(옛 분류)`로 그대로 보입니다.** 새로 고를 수는 없습니다 — 사람이 다시 골라야 합니다.
  `closed`(폐강)는 `schedule` 에 합쳐졌습니다(`WD_MERGED`). 복구 SQL: `sql/fix_wd_reasons.sql`
- 일괄업로드 **양식 파일(`inwon-app/withdraw_template.xlsx`)의 드롭다운도 같이 고쳐야 합니다.**
  코드만 고치면 분원은 옛 목록에서 고르게 됩니다.
- **업로드 결과는 화면에 띄웁니다**(`showWithdrawReport`). 못 찾은 학생·동명이인·
  못 알아본 사유를 이름까지 보여줍니다. 예전엔 `console.warn` 으로만 찍어서
  운영자는 볼 수가 없었고, "올렸는데 갱신이 안 된 것 같다"는 말이 여기서 나왔습니다.
- ⚠️ 전체명단과 마찬가지로 **지난 학기에도 안 막힙니다.** 배너만 뜨고 실제로는 올라갑니다.

### 그 밖에

- 대괄호로 시작하지 않는 행(셔틀버스, 과목만 나뉜 줄)은 통째로 버립니다.
- 운정2는 한 반을 두 선생님이 과목을 나눠 맡아 **IMS에 두 줄**로 들어옵니다.
  대괄호가 붙은 줄이 진짜 담임반이고, 나머지는 버립니다.
- 회원코드가 없는 학생은 `임시-XXXXXX` 코드를 발급하고, 나중에 명단이 올라오면 진짜 코드로 바꿔 붙입니다.
- **`enroll_date`는 NULL이 아니라 빈 문자열(`''`)로 저장된 행이 많습니다.**
  앱은 둘 다 '학기초부터 다닌 학생'으로 읽지만, SQL로 셀 때 `is null` 만 보면 숫자가 완전히 틀립니다.
  반드시 `enroll_date is null or enroll_date::text = ''` 로 보세요.
- ⚠️ **전체명단 업로드는 지난 학기에도 막히지 않습니다.** 화면 배너에는 "덮어쓰기가 잠겨 있어요"라고
  뜨는데 `importRoster`에 `isPastSemester` 가드가 없습니다. 배너와 실제가 다릅니다.

---

## 5. 인원마감표 — 계산이 까다로운 곳

```
월초+신규 = 그 달 시작 인원 + 그 달 신규
학기초   = 재원 + 퇴원 + 전출 − 신규 − 전입
```

### 퇴원율 — 학원 전체가 쓰는 한 가지 규칙 (`wdRatePct` · `wdRateSem`)

```
퇴원율 = 순수 퇴원 ÷ 그 기간에 우리가 데리고 있던 인원

  학기 전체 :  퇴원 ÷ (재원 + 퇴원 + 전출)
  한 달     :  그 달 퇴원 ÷ (월초 + 그 달 신규 + 그 달 전입)
  소수점 한 자리
```

- **전출은 분자에서 빼고 분모에는 넣습니다.** 학원을 그만둔 게 아니라 분원을 옮긴 거지만,
  한때 우리 인원이었기 때문입니다.
- **학기 퇴원율은 누적입니다.** 월별 퇴원율의 평균이 아닙니다 —
  인원이 적은 달과 많은 달이 같은 무게로 들어가 실제와 다른 값이 나왔습니다.
- **분모는 '그 기간에 데리고 있던 사람 전부'입니다.** 학기 도중 반을 통째로 넘겨받았거나
  다른 반에서 학생이 옮겨 왔으면, 그 사람들은 **`신규` 칸에 안 뜨지만 분모에는 들어갑니다**
  (`monthlyClosing`의 `totGained`). 안 넣으면 넘겨받은 반에서 나간 학생까지
  원래 반 인원으로만 나눠서 퇴원율이 부풀어 오릅니다.
  (2026 여름 장안 김세광 선생님 — 7월에 12명을 넘겨받았는데 분모에 안 들어가
   17.2% 대신 21.7%로 나왔습니다.)
- **검산법**: `학기퇴원율 = 총퇴원 ÷ (현재 + 총퇴원 + 총전출)`.
  인원마감표의 어느 줄이든 이 값과 같아야 합니다. 단 **그 선생님 학생이 다른 반으로 옮겨
  갔거나 반을 넘겨준 경우**에는 '현재'에 그 사람이 없어서 이 검산이 조금 어긋납니다 —
  그때는 인원마감표 쪽(데리고 있던 인원으로 나눈 값)이 맞습니다.
- 인원마감표에는 **두 값을 나란히** 보여줍니다 (본사 요청).
  · **학기퇴원율** = 누적. 이번 학기에 전체의 몇 %가 빠졌는지 (`avgRate`)
  · **월평균퇴원율** = 월별 퇴원율의 단순 평균. 한 달에 평균 몇 % 빠지는지 (`meanRate`)
  월평균은 **담당하지 않은 달과 학생이 없던 달을 빼고** 평균냅니다 — 넣으면 0%가 섞여 낮게 나옵니다.
  두 값은 원래 크게 다릅니다(장안 여름 합계 13.1% vs 4.6%). 다르다고 버그가 아닙니다.
- 두 앱에 **글자 그대로 같은 `wdRatePct`/`wdRateSem`** 이 들어 있습니다. 고칠 때 양쪽 다 고치세요.
  예전엔 화면마다 공식이 달라서 같은 분원인데 숫자가 안 맞았습니다
  (통합 대시보드는 분모에 전출을 넣고, 학사 대시보드는 뺐고, 학사 쪽은 정수로 반올림했습니다).

### 반 이동 (`studentMovements`의 `classChange`)

- 이동한 **달까지는 이전 담임** 소속, **다음 달 월초부터 새 담임**. 신규·퇴원 칸엔 안 뜨고 월초만 움직입니다.
- 월초가 바뀐 칸은 노란 배경 + `*` 로 표시하고, 마우스를 올리면 누가 어디로 갔는지 뜹니다.
- **`applyClassMoves()` 한 함수가 강사별·레벨별을 다 처리합니다.** 그룹 축만 갈아끼웁니다.

여기서 실제로 났던 버그들 — 다시 만들지 마세요:

1. **같은 이동이 두 번 저장돼 있으면 한 건으로** 봅니다. 판정은 반 이름이 아니라 **담임 전이**로 합니다
   (같은 이동인데도 저장된 반 이름 문자열이 다른 경우가 있습니다).
   날짜까지 다른 중복은, 정렬 후 **연속으로 같은 전이가 나오면 뒤엣것을 버려** 잡습니다.
2. **한 학생이 두 번 이상 옮기면(A→B→C)** 중간 담임 B는 거쳐 갔을 뿐이라 월초 명단에 없어야 합니다.
   학생별로 모아 날짜순으로 처리하고, 월초 명단은 **맨 처음 담임에게만** 줍니다.
3. **등록하자마자 옮긴 학생은 '이동'이 아니라 '처음부터 그 반'** 입니다.
   9/1에 등록한 신규생을 같은 날 미배정 → 담임반으로 옮긴 걸 이동으로 세면,
   실체 없는 '미배정' 담임 줄이 생기고 다음 달 월초가 음수로 떨어집니다.
4. **다음 달 월초는 하한 0.** 사람 수라 음수가 될 수 없습니다.

**검증 방법**: 그룹별 월초 합계가 분원 전체 합계와 **항상 같아야** 합니다. 어긋나면 버그입니다.

### 담임 변경 (`teacherChanges` — 반 하나가 통째로 다른 선생님에게)

선생님이 그만두면 그 반들을 다른 선생님들에게 나눠줍니다. 학생은 그대로 있고 담임만 바뀝니다.

- 넘겨받은 선생님은 **넘겨받은 달부터** 그 반을 셉니다. 그 전 달에는 **없어야 합니다.**
- 넘겨준 선생님은 **넘겨준 달까지만** 셉니다.
- `monthlyClosing()`에 **`recsAt(월)`** 을 넘겨 "그 달에 이 선생님이 맡고 있던 명단"만 세게 합니다.
  안 넘기면(레벨별·학년별 탭) 예전처럼 학기 내내 같은 명단을 씁니다 — 이 탭들은 담임과 무관합니다.
- 이 달부터 맡는 반이 생겼으면 **월초를 전달 월말에서 물려받지 않고 그 달 명단으로 다시 셉니다.**

⚠️ 예전에 났던 버그 (2026 여름 장안 — 류영임 선생님이 6월까지 근무하고 7/1에 반 4개를 나눠준 건):
**자기 반이 따로 있는 선생님**은 그 반 때문에 모든 달이 '담당 중'이라, 7월에 넘겨받을 반이
**6월 월초에 미리 얹혀 있었습니다.** 넘겨준 선생님에게도 같은 학생이 6월에 잡혀서
6월만 그 반 인원(20명)만큼 이중으로 셌습니다. 넘겨받은 반만 있는 선생님(김원빈·박민욱)은
6월이 통째로 빈칸이라 멀쩡해 보였고, **그래서 한 선생님 숫자만 틀린 것처럼 보였습니다.**

**달 중간(예: 7/15) 교체**는 실제로 흔합니다 (2026 여름 기준 17건 중 10건 — 남동탄·서수원·운정2).
그 달 1일엔 아직 넘긴 선생님 반이므로 **월초는 넘긴 선생님에게만** 붙입니다.
받은 선생님 줄은 그 달 월초가 비고(또는 자기 반만), **다음 달 월초부터** 그 반이 잡힙니다.
대신 받은 선생님의 **그 달 퇴원율 분모에는 넘겨받은 인원을 넣습니다**(`gainedThis`) —
안 그러면 7/20에 나간 학생이 분모 0으로 나눠집니다.
신규·퇴원은 예전대로 변경일 기준으로 쪼갭니다(퇴원은 변경일 당일까지 이전 담임, 다음날부터 새 담임).
예전엔 양쪽 줄에 다 월초를 띄워서 그 달만 강사 줄 합계가 반 인원만큼 컸습니다.

**담임변경 이력에서 조심할 것 두 가지:**

1. **같은 사람인데 표기만 다른 건은 변경이 아닙니다.** 남동탄에 `Stephanie최지영 → Stephanie 최지영`
   (공백만 다름)이 저장돼 있었습니다. 그대로 두면 한 사람이 강사 줄 **두 개로 쪼개지고**,
   같은 반의 진짜 변경(`오은미 → 최지영`)이 아래 2번 때문에 덮여서 사라집니다.
   판정은 `teacherKey()`(한글 이름만 추출)로 합니다.
2. **한 반에 담임변경이 여러 건이면 순서대로 이어 붙입니다(A→B→C).**
   예전엔 `Map`에 덮어써서 **마지막 한 건만 남고 나머지가 조용히 사라졌습니다.**

---

## 6. 고칠 때 이렇게 검증하세요

운영자는 개발자가 아닙니다. **"고쳤습니다"만 말하면 안 되고, 맞는지 확인한 결과를 보여줘야 합니다.**

- **순수 함수는 뽑아서 돌려보기** — `app.js`에서 함수만 잘라내 `new Function(...)`으로 실행.
  (ESM에서 `eval`은 선언이 안 새니 쓰지 마세요.)
- **화면은 실제 브라우저로** — Playwright + Chromium이 깔려 있습니다:
  ```js
  chromium.launch({ executablePath: '/opt/pw-browsers/chromium' })
  ```
  `window.supabase`를 가짜로 심으면 로그인부터 화면까지 다 돌려볼 수 있습니다.
  (외부 CDN은 막혀 있으니 `page.route`로 localhost 외 요청은 abort)
- **실제 데이터로 대조** — 운영자에게 Supabase 조회 SQL을 드리고 결과를 받으면 가장 확실합니다.
  콘솔 스크립트는 iframe 컨텍스트 문제로 자주 실패합니다. **Supabase SQL Editor를 쓰세요.**

---

## 7. 데이터를 고쳐야 할 때

코드만 고쳐선 이미 저장된 게 안 돌아옵니다. `sql/` 폴더에 복구 SQL을 두는 방식으로 해왔습니다.

**항상 4단계로 쓰세요**: `1.조회 → 2.백업 → 3.수정 → 4.검증(0이어야 함)` + 맨 아래 되돌리기 SQL.

```
sql/README_보안.md              비밀번호 보안 적용 순서
sql/auth_stage1.sql             bcrypt 해시 + 로그인 서버 함수 (적용 완료)
sql/auth_stage2_drop_password.sql  평문 칼럼 삭제 (적용 완료)
sql/fix_hc_target.sql           '신입생' 인식 누락 복구 (적용 완료)
sql/fix_two_teachers.sql        담임 두 명 정리 (적용 완료)
sql/fix_wd_reasons.sql          퇴원사유 확정 10개로 정리 (폐강→스케줄 병합)
sql/exam_class_stages.sql       내신반 회차 지정 표
sql/lt_counsel_logs.sql         레벨테스트 상담 기록
```

---

## 8. 권한

| 역할 | 볼 수 있는 것 |
|---|---|
| `admin` | 본사. 전 분원 |
| `branch` | 자기 분원만 |
| `teacher` | 자기 반만, 읽기 전용 |
| `assistant` | 보조 |

- **지난 학기는 삭제·덮어쓰기가 잠겨 있습니다.** 실수로 지난 장부가 날아가는 걸 막기 위함입니다.
  잠금 해제는 **`엄윤경` 계정만** 가능합니다 (`canUnlockPast()`). 새로고침하면 다시 잠깁니다.
- 그 계정은 서수원분원 소속이라 다른 분원을 못 봤습니다. 그래서 **그 계정에만 상단에 분원 전환**을 뒀습니다.
  전환은 그 브라우저 세션에서만 유효하고, 새로고침하면 원래 분원으로 돌아옵니다.
- **비밀번호는 bcrypt 해시로만 저장**되고 브라우저로 안 내려갑니다. 로그인 대조는 서버 함수(`jls_login`)에서 합니다.
  ⚠️ 학생 명단·상담이력 같은 나머지 표는 아직 웹 키만 있으면 읽힙니다. Supabase Auth로 옮기는 건 아직 안 했습니다.

---

## 8-1. 미통과 관리 (서수원분원 전용)

서수원분원만 씁니다. **다른 분원 계정에는 메뉴 자체가 안 보입니다** (`retestOn()` — 분원 이름으로 판정).
학사관리 안에 있고, 화면은 셋입니다.

| 누가 | 무엇을 |
|---|---|
| 조교·주임 | 성적 올리기 + 분원 전체 미통과 보기 (버튼은 못 누름) |
| 담임 | 자기 반만. 조치 버튼을 누르는 사람 |
| 분원 | 전체 보기 + 버튼도 누를 수 있음 |
| 본사 | **안 보입니다.** 서수원 안에서만 쓰는 기능이라 본사 화면에는 아예 안 띄웁니다 |

### 통과 판정 — "사라지면 통과"

큐앱 엑셀에는 **미통과만** 들어 있습니다. 통과 여부를 적어주는 칸이 없습니다.
그래서 **어제 파일엔 있었는데 오늘 파일엔 없으면 통과한 것**으로 봅니다.
9월 1일부터 누적된 전 반 파일 한 개를 하루 한 번 올리기로 했으므로,
**올릴 때마다 그 분원·학기의 `retest_items`를 통째로 갈아끼웁니다.**

**퇴원생은 홈페이지 명단이 기준입니다.** 큐앱 파일에 계속 남아 있어도 알아서 뺍니다.
올릴 때 한 번 거르고(결과창에 이름까지 보여줍니다), **화면에 그릴 때도 한 번 더 거릅니다** —
올린 뒤에 퇴원 처리한 학생이 다음 업로드까지 선생님 화면에 남아 있으면 안 되기 때문입니다.
미조치 알림에서도 빠집니다.

⚠️ 그래서 **일부만 뽑아 올리면 나머지가 전부 통과 처리되어 사라집니다.**
파일이 지난번보다 절반 미만이면 올리기 전에 한 번 더 물어보게 해 뒀습니다(`importRetestFile`).
이 확인창을 없애지 마세요.

### 조치는 따로 저장합니다

담임이 누른 조치(`retest_actions`)는 **회원코드 + 시험열쇠(`item_key` = 구분|회차|교재|단원)** 로 붙여 둡니다.
성적을 다시 올려 목록이 갈려도 조치는 안 지워집니다. 성적만 갈아 끼웁니다.

- 조치는 여섯 가지: 예약 · 왔는지 확인 · 독려 · 학부모 연락 · 보강 · 자료 제공
- **독려와 자료 제공은 누른 횟수를 셉니다.** 그래서 한 번 누를 때마다 한 줄씩 쌓습니다(집계 값이 아니라 이력).
- **'왔는지 확인'은 예약일이 지난 학생에게만 뜹니다.** 왔음/안 왔음을 골라 `memo`에 넣습니다.
  아직 안 온 날짜에 물어봐야 소용없어서 그 전에는 아예 안 띄웁니다.
- **'학부모 연락'은 전화·문자·카톡 중에 고릅니다.** 무슨 연락을 했는지 남아야 나중에 볼 수 있습니다.
- 예약은 누르면 날짜·시간을 물어보고 `memo`에 넣습니다. 파일의 예약보다 이걸 우선합니다.

⚠️ **큐앱 엑셀의 '예약일/시간' 칸은 예약이 없어도 비어 있지 않습니다.** 버튼 글자인
`예약하기`가 그대로 찍혀 나옵니다. 그걸 예약으로 읽어서 68건이 전부 '예약 있음'으로 잡혔습니다.
그래서 **날짜로 읽히는 것만 예약으로 봅니다**(`rtCleanBook`). `예약하기` `미예약` `-` 같은
글자는 예약 없음입니다. 올릴 때 걸러서 저장하고, 화면에 그릴 때도 한 번 더 거릅니다.
- 잘못 눌렀으면 **오른쪽 클릭**으로 한 건 되돌립니다.

### '손 안 댐'의 기준

**그 시험에 아무 버튼도 안 누른 상태**입니다 (운영자가 고른 쪽).
"그날 하루에 한 번이라도 누르면 넘어감"으로 하면 대충 하나 누르고 넘어가게 됩니다.

분원·조교 화면의 최근 7일은 **그 날짜까지 아무 버튼도 안 눌린 시험**을 담임별로 셉니다(`retestMissed`).
지난 날짜를 볼 때는 그 뒤에 누른 조치를 빼고 봅니다 — '그날 퇴근할 때 상태'가 궁금한 거라서.
그날 아직 보지도 않은 시험(`exam_date`가 그 뒤)은 그날의 몫에서 뺍니다.

### 조치 현황 (통과해서 사라진 것까지)

`retest_items`는 올릴 때마다 갈아끼우지만 **`retest_actions`는 손대지 않습니다.**
그래서 통과해서 목록에서 사라진 시험의 조치도 그대로 남습니다.

조교·주임·분원 화면 위에 **`미통과 목록` / `조치 현황`** 탭이 있습니다.
· 담임별로 이번 학기에 무엇을 몇 번 했는지 (학부모 연락은 전화/문자 내역까지)
· 최근 조치 50건 — 통과해서 사라진 건은 이름 옆에 `통과` 표시
· **엑셀로 받기** — 두 장(담임별 합계 / 조치 이력 전체)

사라진 건은 담임 이름을 조치 누를 때 적어 둔 것으로 쓰고, 학생 이름은 전체명단에서 회원코드로 찾습니다.

### 계정 · 읽기 전용

통합관리 › 설정 › 계정 관리에서 지정합니다.

- **직급에 `조교`가 있습니다.** 조교로 만들면 학사관리에서 `assistant` 로 읽혀
  미통과 관리·성적 올리기·STaRT 관리만 보입니다. 만들면 메뉴가 자동으로 그 둘만 잡힙니다.
- 분원 직원(주임·대리 등)은 **인원현황 › 미통과 관리 / 성적 올리기** 를 체크해 줘야 보입니다.
  단, 이미 세부메뉴를 지정해 둔 계정에는 이 키가 없으므로 **새로 체크해 줘야** 합니다
  (세부 지정이 아예 없는 계정은 예전처럼 전체가 열립니다).
- 담임은 세부권한과 무관하게 자기 반 미통과 관리가 보입니다.

⚠️ **담임·주임 계정은 원래 '읽기 전용'입니다**(명단을 못 건드리게 하려고). 그래서 `saveDB`가
통째로 막혀 있어, 조치 버튼을 눌러도·엑셀을 올려도 아무것도 안 남았습니다.

그래서 **미통과 관리는 `saveDB`를 아예 안 씁니다.** 자기 표에만 직접 씁니다.
· `retestSaveAction()` → `retest_actions` (담임이 누른 조치)
· `retestReplaceItems()` → `retest_items` + `upload_batches` (조교가 올린 성적)

**메뉴가 곧 권한입니다.** 미통과 관리·성적 올리기 메뉴가 보이는 계정이면 올리고 누를 수 있고,
그 계정이 명단·상담을 못 고치는 것은 그대로입니다.

두 함수 모두 쓴 뒤에 `db`와 `dbSnapshot`을 같이 맞춥니다. 안 맞추면 나중에 다른 화면에서
`saveDB`가 돌 때 방금 쓴 것을 되돌려 놓습니다.

### 표

`sql/retest.sql` — `retest_items`(지금 미통과인 시험) · `retest_actions`(누른 조치 이력).
둘 다 `optional:true`라 SQL을 아직 안 돌렸어도 앱은 죽지 않습니다.

⚠️ **`optional` 표는 없으면 `saveDB`가 조용히 건너뜁니다.** 그래서 SQL을 안 돌린 채 성적을 올리면
화면에는 "68건 추가"라고 뜨는데 실제로는 아무것도 안 남고, 새로고침하면 0이 됩니다.
실제로 이 일이 있었습니다. 그래서 지금은
· 표가 없으면 두 화면 맨 위에 빨간 배너를 띄우고 업로드 자체를 막고,
· 저장 뒤에 `retest_items`를 서버에서 한 번 세어 결과창에 보여줍니다.
**optional 표를 새로 만들 때는 이 두 가지를 같이 넣으세요.**

### 담임 이름 맞추기

큐앱 파일은 `홍정복/Rachel`, 계정은 `Rachel홍정복` 또는 `Rachel`처럼 표기가 제각각입니다.
`rtTeacherMatch()`가 한글이 있으면 한글로, 한쪽이 영어뿐이면 글자가 서로 품는지로 봅니다.
한글 비교만 했더니 영어 이름만 넣어 둔 계정에서 아무것도 안 떴습니다.
그래도 안 맞으면(파일은 한글만, 계정은 영어만) 담임 화면에 **파일에 적힌 담임 이름 목록**을 띄워
어디가 어긋났는지 보이게 해 뒀습니다.

## 9. 지금 하던 일 (이어서 하실 것)

**CHESS 신규 레벨테스트 시험지 전면 교체**

- `I1` 시험지가 새로 생기고, `D1~D4 · L1~L4` 정답이 **전부** 바뀝니다.
  순서는 `I1 → D1 → D2 → D3 → D4 → L1 → L2 → L3 → L4`.
- 문법·라이팅 시험지도 새로 나온다고 합니다. 예전에 있던 **서술형 문법 10문항이 새 시험지엔 없습니다.**
- **과거 응시자 성적표는 절대 바뀌면 안 됩니다.**
  `level_test_results.result_json`에 채점 결과가 통째로 스냅샷되어 있어서 총점·문항별 O/X는 안전하지만,
  **영역별 배점 표와 루브릭 문구는 현재 코드에서 읽어옵니다.**
  → 지금 시험지 정의를 **v1으로 남기고, 버전 표시가 없는 옛 결과는 v1으로 그리도록** 해야 합니다.
- 고쳐야 할 곳: `leveltest/chess.js`(정답·레벨컷·영역배점·루브릭) · `leveltest/common.js`(시험지 목록·인터뷰 항목·PDF 경로) ·
  `leveltest/chess-bulk.js`(일괄채점) · `leveltest/PDF/` · 루트 `app.js`의 `EXAM_META`·`CHESS_LV_ORDER`
- `getProfile()`의 3영역 배점이 **46%/36%로 하드코딩**돼 있습니다. 적어준 배점대로 계산하도록 같이 고쳐야 합니다.

---

## 10. 운영자와 일하는 법

- **한국어로, 쉽게.** 개발 용어를 쓰면 안 됩니다. `origin`은 "신규/복귀 구분", `class_label`은 "반 이름표"입니다.
- **"목업 먼저 보내봐"** 라고 하면 코드를 고치지 말고 시안을 먼저 보여주세요.
- **"대화 먼저"** 라고 하면 진짜로 코드를 건드리지 마세요. 원인을 설명하고 확인을 받은 뒤에 고칩니다.
- **버그 신고는 대개 스크린샷**으로 옵니다. 화면 숫자를 역산해서 원인을 좁히는 게 빠릅니다.
- **추측해서 고치지 마세요.** 확인용 SQL을 드리고 실제 데이터를 받아서 고치는 편이 훨씬 빠릅니다.
  추정으로 밀어붙였다가 크게 틀린 적이 여러 번 있습니다.
- 고친 뒤엔 **터미널에 넣을 명령어**를 그대로 적어주세요.

---

## 11. 코드에 주석을 남기세요

이 저장소의 주석은 **"무엇을"이 아니라 "왜, 예전엔 뭐가 잘못됐는지"** 를 적습니다.
운영자가 개발자가 아니고 유지보수를 이어받는 사람도 맥락이 없기 때문입니다. 이 방식을 지켜주세요.

```js
/* 특이사항은 분원마다 말이 다르다 — '신입생' '9월 신규생' '복귀생' 전부 같은 뜻으로 읽는다.
   예전엔 '신규' 네 글자만 찾아서 '신입생'이라고 쓴 학생이 기존생으로 떨어졌고,
   그 바람에 해피콜(HC)을 다 해놓고도 상담률에 안 잡혔다. */
```
