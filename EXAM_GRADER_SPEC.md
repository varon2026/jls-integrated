# 시험채점기 재설계 — 설계·진행 인수인계 문서

> 이 문서는 어느 컴퓨터(집/회사)에서든 작업을 이어가기 위한 **단일 기준 문서**다.
> 새 대화창에서 작업을 이어갈 때 Claude에게 "이 파일(EXAM_GRADER_SPEC.md) 읽고 이어서 하자"라고 하면 된다.
> 작업 폴더: `C:\Users\Desk3\jls-integrated` (GitHub: varon2026/jls-integrated). 다른 컴퓨터에선 먼저 `git pull`.

---

## 0. 지금까지 한 것 / 남은 것 (현재 상태)

- ✅ **원무 홈페이지의 "시험채점" 카드 임시 숨김** — `app.js`의 카드 블록 주석 처리. 커밋 `5e06ef7` main에 푸시됨.
  - ⚠️ 단, **GitHub Pages 배포가 성공해야 실제 사이트에 반영**됨. 현재 GitHub Pages 배포가 불안정(실패)한 상태라, 홈페이지 계산식 수정(`050fc5f`)과 이 버튼 숨김(`5e06ef7`)이 **다음 성공 배포 때 함께 반영**된다. 다른 분원 오픈 전에 배포 1회 성공 필수.
  - 되돌리려면: `app.js`에서 "새 채점 페이지 완성 전까지 임시 숨김" 주석 블록을 풀면 다시 보임.
- ⬜ **새 채점기(단독 HTML) 제작** — 아직 시작 전. 아래 설계대로 만들 예정.

### 개발/배포 방식 (합의됨)
- 채점기는 **자체완결 HTML 파일 1개**로 만든다. Supabase에 직접 연결 → 로컬에서 파일 더블클릭으로 실제 데이터 테스트 가능.
- **완성 전까지 GitHub 배포 안 함.** 로컬에서 열어보고 고치기를 반복해 완성. GitHub Pages 배포 문제와 완전 분리.
- 완성 + GitHub 정상화되면 그때 사이트에 올리고 원무 "채점" 버튼을 새 페이지로 연결(숨김 해제).

---

## 1. 목표

원무에서 학생들의 **DT / AT / 내신모의고사** 답안을 입력하면 **즉시 채점(맞음=초록/틀림=빨강) + 영역별·최종 점수**가 나오고, 저장 후 **다시 열어 수정**도 되는 단독 페이지. 인원현황·레벨테스트처럼 버튼 누르면 **새 페이지**로 뜬다.

## 2. 시험 종류 → 레벨 매핑

- **DT = CHESS 시험** → CHESS 레벨만 노출
- **AT = ACE 시험** → ACE 레벨만 노출 (단, 수능 배우는 반은 **모의고사=사설시험**이라 이 시스템 채점 대상 아님)
- **내신모의고사 = 초6·중1 시험** → 반명에 `E6`, `M1`, `초6`, `중1`, `초등6`, `중등1` 중 하나가 들어간 반만 불러옴

레벨 선택 시 해당 종류의 레벨만 나와야 함.

## 3. CHESS 레벨 체계 (DT)

접두사: **IS, DS, LS, MS**
- **IS**: `IS1`, `IS2` (2개뿐)
- **DS**: 세부 A~D × 숫자 1,2 → `DSA1,DSA2, DSB1,DSB2, DSC1,DSC2, DSD1,DSD2`
- **LS**: 세부 A~D × 숫자 1,2 → `LSA1,LSA2, LSB1,LSB2, LSC1,LSC2, LSD1,LSD2`
- **MS**: 세부 A,B × 숫자 1,2 → `MSA1,MSA2, MSB1,MSB2` (여기까지)
- 숫자는 항상 1 또는 2. 예시 레벨: `IS1`, `MSA1`, `DSD2`

이 접두사(IS/DS/LS/MS)가 아니면 전부 **ACE**로 본다. **ACE 레벨 체계는 CHESS 완성 후 별도로 받는다.**

## 4. 화면 구조 (단독 페이지 · 크고 깔끔하고 쉽게)

- **왼쪽 사이드바**: `DT` / `AT` / `내신모의고사`
- 종류 클릭 → 사이드바 하위에 항목 2개:
  1. **답안입력** (학생 답안 채점)
  2. **정답등록** (정답키 입력) — *이름은 확정 필요*. 정답을 답안입력 화면에 같이 두면 지저분해서 **분리**한다.

### 4-1. 정답등록 화면
- **레벨별로** 정답 입력. 화면에서 직접 입력(객관식 1~5). 문항마다 **영역(예: 리스닝/독해)·배점** 지정.
- **일괄등록**도 지원 → 일괄등록용 **엑셀 양식은 Claude가 제공**. 컬럼: 영역별 · 배점 · 정답(점수) 입력 가능.
- 정답은 **학기가 바뀌어도 계속 유지**. 필요 시 **수정 가능**.
- 영역 세분화 예: `1~10 리스닝, 11~20 독해` — **레벨별로 사용자가 시험지 보고 직접 채워넣음**(그래서 Claude는 시험지 없이도 틀을 만들 수 있음).

### 4-2. 답안입력 화면
- 상단에 **레벨 선택** (해당 종류 레벨만).
- 레벨 선택 → 하단에 **학생 명단**이 쭉 뜸. 명단 컬럼: **학생명 · 학교 · 학년 · 회원코드**.
- 명단 옆에 **답안 입력칸**. **상하좌우 방향키**로 셀 이동.
- 답 입력 **즉시** 맞으면 **초록**, 틀리면 **빨강**.
- 점수: **영역별 점수** + 맨 끝 **최종 점수**.
- **재시험**: 재시험 보는 학생 있음 → **재시험 버튼** 누르면 새로 답안 입력.
  - **1차 점수는 그대로 보존**(1차 답안은 저장 안 해도 됨).
  - **2차는 답안 + 2차 점수** 저장.
- **미응시 버튼**: 내신모의고사 등은 학기마다 응시 대상이 달라짐(어떤 학기엔 초6 안 보거나 중1만 보거나…). 매 학기 자동화 불가 → 레벨을 불러온 뒤 **그 레벨이 시험 안 보면 미응시 버튼으로 수기 체크**.

## 5. 데이터

- 업로드된 **전체명단(semester_records + students)** 기준으로 DT/AT/내신 **응시 인원을 자동 추출**.
- Supabase 프로젝트: `https://hplndiuoohantbalixwu.supabase.co` (publishable key는 앱에 내장).
- 관련 기존 테이블: `exam_keys`(정답키), `exam_scores`(학생답안·점수), `exam_config`(반별 응시여부). 재사용 여부/스키마는 재설계 시 확정. **저장은 반 단위 upsert(중복 방지)로 → 다시 열어 수정 가능**하게.
- 점수 = **맞은 배점 ÷ 총 배점 × 100** (확인됨).
- 반↔레벨 매칭은 **반 이름 기준 자동매칭**.

## 6. 참고: 코드 위치

- 원무 통합앱: 루트 `app.js` (원무 대시보드, 채점 카드 = 숨김 처리한 곳)
- 기존 DT/AT 채점기(구버전, 갈아엎을 대상): 루트 `grader.html`
- 레벨테스트(예약자 채점, 별개 시스템·유지): `leveltest/` 폴더, `app.js`의 openScoreTest/openScoreView
- 인원현황: 루트 `inwon.js`

## 7. 다음 할 일

1. 새 채점기 **자체완결 HTML 파일** 뼈대 생성(사이드바 DT/AT/내신 → 답안입력·정답등록, CHESS 레벨 목록, 명단 자동추출, 답안 그리드 실시간 O/X·영역별 점수, 재시험, 미응시, 정답등록 UI + 일괄등록 엑셀 양식).
2. 로컬에서 열어 테스트 → 피드백 반영 반복.
3. 사용자가 시험지 보고 레벨별 정답·영역 입력. ACE 레벨 체계 확정 후 반영.
4. 완성 + GitHub 정상화 시 배포 + 원무 채점 버튼 연결(숨김 해제).

---

## 부록 — 내일 기타 할 일 (채점기 외에도 잊지 말 것)

1. **★ 인원 맞추기** — 목표 숫자: **장안분원 = 449**, **서수원분원 = 343**.
   - 배포 성공 후 홈페이지에서 이 숫자로 맞는지 확인.
   - 참고: 이전 분석에선 서수원 "여름 시작"이 344로 계산됐었음(오윤진 st_2b2h7cg 포함). 목표가 343이라면 오윤진 처리/영역을 내일 다시 확인해 344 vs 343 어느 쪽이 맞는지 확정할 것.
   - 장안 449는 신희우(st_ci5hkqu, 복귀·재원·퇴원일 2026-03-30) 포함이 맞음(확인됨).

2. **교재(books) 데이터 복구** — Supabase를 이전 사람 계정 → 회사 계정으로 옮길 때 **교재 테이블·데이터가 안 넘어옴**(현재 프로젝트 hplndiuoohantbalixwu 엔 인원·상담 데이터만 있음, books/sales_history 등 없음). books-app 코드는 정상.
   - 할 일: 예전 계정의 Supabase 접근 가능한지 확인 → `books`(및 sales_history·stock_by_branch·vendors) 데이터 Export → 현재 프로젝트에 테이블 재생성 + 데이터 이관. (엑셀 백업 있으면 그걸로도 가능.)
   - books 테이블 컬럼(코드 기준): barcode, name, subject, publisher, vendor, price, purchase_price, note, level, level_namdongtanjls.

3. **GitHub Pages 배포 성공시키기** — 홈페이지 계산식 수정(`050fc5f`) + 채점 버튼 숨김(`5e06ef7`)이 아직 라이브 반영 안 됨(현재 Pages 배포 실패 중). **다른 분원 오픈 전 배포 1회 성공 필수.** GitHub Actions 에서 실패한 "pages build and deployment" #41 **Re-run** 또는 재배포.
